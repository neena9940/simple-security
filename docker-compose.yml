version: '3.8'

services:
  log-collector:
    build: ./log-collector
    ports:
      - "8005:8005"
    networks:
      - security-net

  analyzer:
    build: ./analyzer
    ports:
      - "8006:8006"
    networks:
      - security-net

  dashboard:
    build: ./dashboard
    ports:
      - "8007:8007"
    networks:
      - security-net

networks:
  security-net:
    driver: bridge