version: '3.8'
services:
  log-collector:
    build: ./log-collector
    ports:
      - "5006:5006"
    restart: unless-stopped

  analyzer:
    build: ./analyzer
    ports:
      - "5007:5007"
    restart: unless-stopped

  dashboard:
    build: ./dashboard
    ports:
      - "5008:5008"
    restart: unless-stopped